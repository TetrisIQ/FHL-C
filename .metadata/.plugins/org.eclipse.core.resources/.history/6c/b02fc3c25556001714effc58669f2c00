/*
 * anacae.c
 *
 *  Created on: 20.06.2017
 *      Author: alex
 */

#include "anacae.h"
#include "caesar.h"
#include <string.h>
#include <stdio.h>
#include <ctype.h>
#define MAX 27

int anacae(char* text) {
    int i = 0;
    int highestValue = 0;
    size_t size = strlen(text);
    char buffer[size];
    char anzahl[MAX];
    int often[MAX] = {
    0
    };
    int position = 0;
    strcpy(buffer, text);
    while (buffer[i] != '\0') {
        if (buffer[i] < 97) {
            buffer[i] += 32;
        }
        findMostUsed(buffer[i], anzahl, often);
        i++;
    }
